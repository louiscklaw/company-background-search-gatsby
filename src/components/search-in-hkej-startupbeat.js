import React from 'react'
import LinkToOutside from './link-to-outside'

function SearchInHkejStartupbeat(props){

  return(
    <div>
      <LinkToOutside href={`http://startupbeat.hkej.com/?s=casetify`} text={`search in hkej startup beat`} />
    </div>
  )
}

export default SearchInHkejStartupbeat